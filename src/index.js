import React from 'react';
import ReactDOM from 'react-dom/client';
import LuckyRanking from './components/LuckyRanking';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <LuckyRanking />
  </React.StrictMode>
);